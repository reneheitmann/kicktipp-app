# Logs (nur der Root-Ordner, nicht z. B. src/features/logs/)
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Lokaler Supabase-CLI-Cache (Projekt-Link, Pooler-URL etc.)
supabase/.temp

# Entschlüsselte DB-Backups (siehe .github/workflows/db-backup.yml) –
# landen hier nur temporär beim lokalen Entschlüsseln, nie eingecheckt.
/backup

# Claude Code lokale Einstellungen (können Secrets aus vergangenen
# Tool-Aufrufen enthalten, z. B. in Permission-Regeln mitgeloggte curl-Befehle)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
